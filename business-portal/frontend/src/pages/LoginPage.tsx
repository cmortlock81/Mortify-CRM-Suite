import {useState} from 'react'; import {post} from '../api/client'; export default function LoginPage(){const [email,setEmail]=useState('admin@example.com'),[password,setPassword]=useState('ChangeMe123!'),[err,setErr]=useState(''); async function login(e:any){e.preventDefault(); try{const r=await post('/auth/login',{email,password}); localStorage.setItem('token',r.token); location.href='/dashboard'}catch(e:any){setErr(e.message)}} return <form onSubmit={login} className="login"><h1>Sign in</h1><input value={email} onChange={e=>setEmail(e.target.value)}/><input type="password" value={password} onChange={e=>setPassword(e.target.value)}/><button>Login</button><p>{err}</p></form>}
