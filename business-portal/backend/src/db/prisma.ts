// SQL migrations are the source of truth. This placeholder documents where a PrismaClient
// could be initialised if the reference schema is used for typed read models later.
export const prisma = null;
